# RST-005 Execution Report

## Status

`EXECUTED`

RST-005 was executed and independently verified against the shared tenant on
2026-09-01. The execution was bound to committed implementation
`b9520f5aaf38629d13618034cce546e71637ebab`, protected-tree digest
`a01bfd02d6e0bff4c1039f5f191233bfa5fe9cbc170c715f640737d75403f40f`,
and protected manifest digest
`cd1a98dddfa6f0b3cb65553f25d5212520196f8bb4d0ffb7b08c092419176884`.
The exact committed tree passed `npm run test:rst005-launcher`,
`npm run test:rst005` (4/4 Playwright cases), `npm run test:verify`, and
`npm run test:unit` (127/127), plus syntax and whitespace checks. Final
Standards, Spec, Security/Isolation, and full-feature reviews reported no
blocker before the separate commit/tree execution approval was issued.

The operational script accepts shared mutation only through a root-owned
authorization record generated by the protected launcher. The launcher opens
approval, the 32-byte encryption key, traffic-stop attestation, and canonical
Compose environment itself from four fixed, separate root-owned `0400` names
in root-owned `0700` custody,
using no-follow opens so symlink-origin substitution is rejected;
binds exact commit, clean protected tree, Compose bytes/resolution, tenant,
database, document root, and backup/evidence roots; requires Dolibarr stopped;
streams encrypted backups; restore-tests them in a nonce-scoped isolated
MariaDB container; and verifies target, finalized, and containment-restored
evidence. The key is passed onward only through dedicated inherited file
descriptors. Approval/key bytes, Git/tree, Compose topology/digest, target,
approval/traffic expiry, and stopped-service state are rechecked immediately
before client setup, each backup, and every mutation. No path argument or
environment flag alone enables shared mutation.

The v3 approval separates stable target identity from operation-specific
execution identity, proves an active inherited `flock --nonblock --no-fork`
lock plus independent contention, and gives every mutating daemon child a
stable mutation lease,
binds the nonsecret protected-tree manifest, real Compose plugin executable,
database image/datadir/filesystem identity, approval/traffic relation, and
exact shared or disposable-shared-shape profile. One-offs use the approved
immutable image ID with pulls disabled and a create-inspect-start boundary,
read-only root, tmpfs configuration, all capabilities dropped except
`DAC_READ_SEARCH`, no-new-privileges, exact mounts/network/entrypoint, and
auto-removal. Recovery writes a fresh complete current-state manifest before
containment mutation.

The shared tenant is now on the empty finalized target Activity foundation.
This report records the `EXECUTED` verdict; it authorizes no downstream unit,
rollback, artifact destruction, or restoration of an unknown state.

## Current confidence-hardening verification evidence

### 2026-08-28–31 RST-005 root-cause correction

The retained pre-execution packet was read only through network-isolated,
disposable Docker resources. Its encrypted backup and key were used only
inside those resources. Neither the key nor decrypted database was printed,
exported, or retained outside the disposable verification boundary, and the
packet was not modified.

The packet-backed reproduction localized closed stage 215 to a byte-level
evidence mismatch: `tests/fixtures/database-evidence.php` encoded a nullable
field with the literal two characters `\` and `n`, while the migration-side
evidence encoder used an actual LF byte. Empty disposable ECM tables had
previously hidden the divergence. Corrected packet-backed apply reached
`cutover_complete_pending_finalize` in a network-isolated disposable tenant.

The launcher-interrupt failure was a convergence race, not an isolation
defect. Docker could continue reporting an auto-removed container or dependent
network while removal was already in progress. Production cleanup now performs
at most 300 exact-container removal/observation rounds before at most 300 exact
network/volume rounds, with a 100 ms delay between rounds; Docker command time
is additional, and permission failures still abort. The
test harness independently waits on read-only categorical survivor queries and
retains its separate exact-resource finalizer, so a persistent survivor still
fails the scenario. Production survivor observations are strict: any Docker
observation error aborts instead of being interpreted as resource absence.

At the 2026-08-28–31 pre-commit checkpoint, the final reduced correction
intentionally contained only the LF-byte parity fix and bounded Docker cleanup
convergence. Its then-pending committed-source gates were:

- `npm run test:rst005-launcher`;
- `npm run test:rst005`;
- `npm run test:verify`;
- `npm run test:unit`;
- changed PHP/Node syntax checks and the exact correction-range whitespace check.

No shared execution, shared rollback, shared database/document change, or
retained-packet mutation had occurred at that checkpoint. The later commit
identity, protected-tree digest, final reviews, secret scan, zero-survivor
audit, and shared execution are recorded by the status and shared evidence in
this report.

### Superseded 2026-08-27 evidence

The current uncommitted correction tree passed the following on 2026-08-27:

- `npm run test:rst005-launcher` — one complete, uninterrupted matrix passed.
  It covered harness and launcher SIGHUP/SIGINT/SIGTERM; source mutation before
  and after validation; protected-input replacement; lock contention; foreign
  network peer and alternate database network; ancestor filesystem writer;
  enabled SQL event; foreign database session; the complete input/topology/
  dirty-tree rejection set; positive rehearsal; all durable
  `execute -> SIGKILL -> recover` prefixes including manifest publication and
  finalization; complete execute plus separately approved rollback; SIGKILL
  while apply/recover/rollback held the mutation lease; plaintext-restore
  SIGKILL lifetime and fresh-launch reaping; and missing-chain-head, duplicate,
  reordered, truncated, corrupt, copied/replayed, and contradictory durable
  records. Exact scenario teardown completed.
- `npm run test:rst005` — passed in 422.9 seconds after correcting a dead
  disposable verification-failpoint guard. Exact cutover, restart, activation,
  finalization, schema, audit, and containment gates passed; all 4 Playwright
  tests passed; the tenant's containers, volumes, and network were removed.
- PHP syntax sweep — every PHP file under `custom/mjlfinancement` and
  `tests/fixtures` passed `php -l`.
- Relevant Node syntax checks, `git diff --check`, and the focused
  `rst005-activity-foundation` plus `rst005-confidence-hardening` unit files
  passed.
- secret verification — the exact 32-byte backup key served as the canary and
  was absent from captured stdout/stderr, snapshots, journals, reports,
  retained artifacts, and orphaned one-off metadata/logs. A source scan found
  no private-key, AWS access-key, GitHub-token, or OpenAI-style secret
  signatures.
- Four stale, label-proven disposable RST-005 projects from earlier
  interrupted/focused runs were removed by exact name: seven containers, four
  networks, and six volumes. The final RST-005 survivor audit was empty. No
  shared resource was included.

The root-only shared packet generator was implemented and statically/unit
verified but was not executed. Shared inspection, traffic stop, packet
generation, backup, DDL, recovery, rollback, and resource deletion remain
outside this authorization.

`SECURITY/ACCESS-BLOCKED — DO NOT RETRY`:

- direct/sandbox `node tests/unit/rst005-shared-launcher.test.js` could not
  spawn its Git/PHP children;
- the later sandbox `npm run test:unit` failed for the same child-process
  restriction;
- sandbox `npm run test:verify` could not open its required localhost listener
  (`listen EPERM 127.0.0.1`).

Those exact blocked paths were not retried. The earlier 123/123 unit and
complete verification passes predate the final source corrections and are
therefore superseded rather than presented as current evidence. The focused
current unit files and both authorized disposable RST-005 suites are current.

Preliminary Standards, Spec, and Security/Isolation reviews found stale index
wording plus lock, orphan-mutation, immutable-image, chain-grammar, recovery,
authorization, Compose/runtime, partial-state, canary, and operational-matrix
gaps. Those findings drove the correction. Repeat reviews were clean before
the final disposable verification-failpoint correction. Final Standards, Spec,
and Security/Isolation reviews against the complete current tree are `CLEAN`.
At that pre-execution checkpoint, full-feature validation was `PASS` for the
disposable implementation and exact shared-launcher code path, while the
shared tenant was still `NOT_EXECUTED/UNAUTHORIZED`; DEC-047 and the shared
evidence below supersede only that final execution status.

## Approved guarded-lock amendment

MariaDB 11 rejects `RENAME TABLE` while the session holds explicit table locks.
The revised cutover therefore installs one exact temporary `BEFORE INSERT`
denial trigger on the empty Phase 1 Activity table, obtains and releases the
sealed table-lock set only after repeating every premise, and immediately runs
the atomic rename while that guard still excludes direct writers. The guard
moves with the quarantine table and is removed by exact name. Crash recovery
accepts only the verified guarded forms documented in the strategy.

The implementation also seals both complete retained-schema forms caused by a
historical nonsemantic column-order difference: Phase 1 appended the generated
`user_role.active_user_id` column, whereas clean installation creates it beside
`is_active`. No third digest and no retained-table repair are accepted.

## Superseded pre-launcher implementation evidence

The pre-launcher runtime/test tree passed these gates on 2026-08-24. These
results are historical because the protected launcher was hardened afterward:

- `npm run test:rst005` — passed in 428.6 seconds. Encrypted schema/full
  backups restored with exact dump and restorable-logical equality; randomized
  restore databases and the alternate-prefix scratch database left neither
  schemas nor `mysql.db` grants; non-disposable restore and mutation callers
  were rejected; all failpoints, restart recovery, activation, finalization,
  retry, and containment-only rollback paths converged; exact schema and audit
  verification passed; 4/4 Playwright tests passed; whole-tenant teardown
  removed all containers, volumes, and network.
- `npm run test:verify` — passed in 156.7 seconds and removed its isolated
  tenant after the exact Phase 1 empty-schema verifier passed.
- `npm run test:unit` — passed 88/88 Node tests plus maintained PHP contract
  checks.
- PHP lint — every changed and new PHP file passed `php -l`.
- `git diff --check` — passed.
- Independent Standards review — `CLEAN`.
- Independent Spec review — `CLEAN`.
- Independent Security/Isolation review — `CLEAN` for the current
  disposable-only/pre-execution tree.

The full-feature validation result is `PASS` for disposable implementation and
the exact shared-launcher code path, and `NOT_EXECUTED` for the shared tenant.
No shared preflight, backup, DDL, finalization, rollback, or state mutation
occurred.

## Superseded protected-launcher implementation evidence

On 2026-08-25, the then-current uncommitted tree passed `npm run test:unit` (105/105),
`npm run test:rst005` in 491.3 seconds (4/4 Playwright plus complete teardown),
and the public `npm run test:rst005-launcher` command. The exact launcher
scenarios passed: a real `SIGTERM` harness teardown (exit 143); termination of
the exact root launcher after the database client and isolated restore
container, network, and volumes existed, followed by launcher-owned zero-
survivor checks; the exact missing-input/short-key/symlink/weak-custody/wrong-
commit/protected-manifest/Compose/target/shared-target/stale-traffic/dirty-tree
rejection matrix; the full disposable encrypted-backup/restore/apply/
activation/finalize/containment-rollback path; and an isolated shared-shaped
bind tenant executing the production `--mode=execute` path followed by a fresh
approval and the standalone production `--mode=rollback` path. The latter
proved the root-only ephemeral one-off configuration seam while application
code and documents remained read-only; it never addressed the real shared
project or shared bind paths.

The retained launcher report now records explicit Phase 1/target Activity
schema identity, row and column counts, Activity table digest, audit count and
digest, exact temporary-table inventory/absence, complete database delta,
module metadata, document, ECM, Admin, protected projection, backup, and
Compose-resource digests. Every scenario removed its tenant, one-offs, restore
resources, custody root, and ephemeral database client configuration. The
public run also proved explicit noninteractive stdin closure for nested Compose
evidence processes. A survivor audit found and removed one exact stale nonce-
scoped disposable restore set from a 2026-08-24 diagnostic run; repeated audits
and all final scenario teardowns were clean. DEC-046 supersedes every one of
these results. No post-hardening result is current until the exact correction
commit passes the complete gate and review matrix.

## Superseded diagnostic evidence

Earlier implementation checkpoints on 2026-08-24 passed the following gates,
but are superseded by later source hardening and are not the final evidence
boundary:

- `npm run test:verify` — passed in 111.3 seconds; clean installation and exact
  Phase 1 verification converged, followed by whole-tenant teardown.
- `npm run test:rst005` — passed in 387.5 seconds; encrypted streaming schema
  and full backups restored with matching plaintext digests in fresh processes;
  all target-object, guarded-cutover, activation, verification, finalization,
  retry, and rollback failpoints converged; complete database/filesystem/native
  ECM evidence matched after reversible rollbacks; 3/3 Playwright tests passed;
  whole-tenant teardown passed.
- `npm run test:unit` — passed 88/88 Node tests plus the maintained PHP contract
  checks.
- PHP lint — every changed and new PHP file passed `php -l`.
- `git diff --check` — passed.

One earlier disposable run stopped before migration DDL because its source-hash
walker misclassified the deliberately excluded orchestrator as a non-file. The
runner branch was corrected, `/tmp` backup/key absence was verified, and the
complete suite then passed. Another clean-install diagnostic tenant exposed the
sealed `active_user_id` ordinal difference; it and all isolated volumes were
explicitly destroyed after inspection.

Later diagnostic runs found and corrected MariaDB-generated trigger timestamp
drift, restore-schema identity drift, missing metadata visibility under
SELECT-only grants, unsupported `REVOKE IF EXISTS` syntax, and a residual
alternate-prefix database grant. Each failed disposable tenant was removed.
Only the final evidence above is the current validation boundary.

## Shared execution evidence

### Approval, packet, and maintenance boundary

The exact user approval was:

> I approve RST-005 shared packet generation and execution at commit
> b9520f5aaf38629d13618034cce546e71637ebab with complete_tree_sha256
> a01bfd02d6e0bff4c1039f5f191233bfa5fe9cbc170c715f640737d75403f40f,
> including containment-only recovery with the original packet after a valid
> incomplete durable prefix and one replacement packet bound to the same
> b9520f5aaf38629d13618034cce546e71637ebab/a01bfd02d6e0bff4c1039f5f191233bfa5fe9cbc170c715f640737d75403f40f
> only if the original expires or fails before any durable manifest and
> independent evidence proves exact Phase 1/no DDL. This does not approve
> rollback, unknown-state restoration, or artifact destruction.

- The approval named the exact commit/tree pair above and permitted only the
  stated containment recovery and one tightly bounded replacement packet.
  It was received before the original packet was issued at
  2026-09-01T13:50:58Z; the repository has no more precise durable chat-event
  timestamp, so none is invented here.
- Dolibarr traffic was stopped before packet execution; MariaDB remained
  available only on the project network. The first traffic record was issued
  at 2026-09-01T13:53:01Z.
- The original root-owned packet
  `/tmp/mjl-rst005-shared-b9520f5-20260901T135030Z` failed before the launcher,
  backup, durable manifest, or DDL because its container-generated invocation
  referenced unavailable host Node path `/opt/node` (`flock` exit 69).
- Independent read-only evidence proved exact Phase 1, no temporary table, no
  backup, and no durable manifest. This satisfied the user's narrowly approved
  replacement condition; it was not a recovery, rollback, or unknown-state
  restoration.
- The single replacement packet
  `/tmp/mjl-rst005-shared-b9520f5-replacement-20260901T135623Z` was issued at
  2026-09-01T13:56:40Z with the same approved commit/tree and stable target
  identity. Its traffic record was issued at 2026-09-01T13:57:36Z.
- Shared durable execution ran from 2026-09-01T13:59:29Z through
  2026-09-01T14:00:13Z. Dolibarr restarted at
  2026-09-01T14:06:49Z and the local HTTP endpoint responded successfully.

The successful packet binds operation
`0bab6b74e91baa32361ef28498a60ffd`, target identity
`8b3a0dbef289fb2c4d9f6b66a32ab884ccfb83fe5d3e99ca30392c8247fd3c8d`,
execution identity
`bee988bfe5f424598fd26db44158a44329d855c31e9fe3e06f5cf20b2dfdbe59`,
evidence manifest
`154f3bfcd4f0dcf77ef9f0635f8fc019b004f663bed6d0cd3d645605ace9e24a`,
and launcher-report digest
`f6d4de3664a5c39e733b63242032d34ca6f29286cac36eb38a1015566fea2e63`.
The immutable chain is exactly `manifest-before -> checkpoint-before-apply ->
checkpoint-before-activation -> manifest-target ->
checkpoint-before-finalize -> completed-report`.

### Before/after and independent verification

- Activity changed from the exact empty 22-column Phase 1 schema to the exact
  empty 20-column target schema. Classification is `finalized_target`, target
  verification is `verified`, and no temporary RST-005 table remains.
- The exact database delta contains only `llx_const`, `llx_menu`,
  `llx_mjlfinancement_activity`, `llx_user_rights`, and trigger definitions.
  No business or audit row was created.
- Module metadata changed deterministically from
  `0fcd2b6d122e8cb7ab915b77484d990c49ee5eb2e9683a75c5712d52c0b771b9`
  to `4558cae96987e394b70aefb3342df4dd0039745e439798f6eed4f97060c3b536`
  through the approved module activation delta; embedded module source stayed
  bound to `6dd2e4c3f6a320eb47e100d58a5ef89b28d3348742cce969828e9a162d31f014`.
- Complete database digest changed from
  `1fe8a6f5ca209a4d3e10ab04ab32bcd2563035caf989ba3fb1f915ba9e8fc8bd`
  to `6c4d3a1b07d5a21e5a77a123261fb9997c2b6443d5d8a992191feb5c977ee54a`;
  protected-table digest changed from
  `9d89de4d84be17d3c1e91853f6ff666c485ddb5251ad977e9660efe1f65403a6`
  to `87663f969515948f31840fd653d69bae9ce0974cb386f93c83f7e5a1ef6dc6e1`
  only through the sealed delta above.
- Admin
  (`929aa95ba1d67a44d5deaca6b8c16eff0e00b6fcebe2a8761d7468171d44319a`),
  documents
  (`e4c1b16e2a77ca669e87db6f62639b572f95fd826206f6539dc6fdb45feca2e7`),
  native ECM
  (`3120862f8f121c0a0122b453ab616b43671479aeb9199898f24658042fce0a2e`),
  Activity audit
  (`0e5652139eea4ba9c25bd8ee411010be74250ff58e6a31046a01cb60033c2142`),
  and Compose resources
  (`7b4dcbc56e313d14d570e3295214f3b1ee0a3c8a43f832e8a0b0234b53bf3b83`;
  two containers, one network, zero named volumes) are unchanged.
- Independent live evidence confirms one active native technical Admin
  (`llx_user.rowid=1`), zero non-Admin users, and zero Partners, Projects,
  Activities, Opérations types, assignments/scopes, invitations, password
  resets, ECM files, and audit events.
- Encrypted schema and full backups were captured and independently restored;
  schema plaintext digest is
  `d22736e92a0926098a437e62f2009d2c9357cd300866183c121e6fae735b6c62`,
  full plaintext digest is
  `157a95141c2a1bd37b626774184d9bc82be034626cf742a2ab8ffe5c8f80abb8`,
  and restored logical database digest is
  `73e2f1f7efa4044b2b785e590231347772d7f058196f9e4b9edbbe24fc6d82c8`.
  No plaintext backup or key was emitted.
- Final survivor checks found no replacement-nonce container, one-off,
  network, or volume. Both packets, encrypted backups, key, immutable records,
  and locks were retained; no artifact was destroyed.

### Commands, results, and intentionally skipped actions

- `docker compose --env-file /dev/null -f docker-compose.yml stop dolibarr` —
  passed; only Dolibarr stopped and MariaDB remained running.
- The packet's immutable generated invocation recorded in `invocation.json` —
  the original exited 69 before the launcher; the single authorized
  replacement returned `executed_target_finalized`.
- Read-only `rst005_activity_foundation.php --mode=classify` and
  `--mode=verify` one-offs — passed as `finalized_target` and `verified` with
  zero rows and no temporary table.
- Read-only `database-evidence.php` — passed after supplying its omitted
  read-only fixture mount; hashes and zero-state counts matched the completed
  report.
- Exact-name Docker survivor queries — passed with zero nonce-bound container,
  network, volume, or one-off.
- `docker compose --env-file /dev/null -f docker-compose.yml start dolibarr`
  plus `curl --fail http://127.0.0.1:8080/` — passed. The first startup probe
  reset while Apache initialized; the next probe succeeded.
- `recover` was skipped because no valid incomplete durable prefix existed.
  `rollback` was skipped because it was not approved. Unknown-state restore and
  artifact destruction were neither approved nor attempted.

The first database-evidence invocation after execution omitted its read-only
fixture mount and failed before evidence collection. It was rerun with the
approved fixture mounted read-only and passed. No mutation command was retried.
Shared rollback and recovery were not run because the completed target was
healthy. No downstream Phase 2 unit is authorized by this execution report.
